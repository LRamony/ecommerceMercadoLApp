
<div class="col-lg-12">
    <h1 class="page-header">Admins</h1>
</div>

<!-- /.col-lg-12 -->
<div class="col-lg-12">
            <!--
            <% if notice %>
                <div class="alert alert-success"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">
                            <%= notice %> </font></font><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">
                        </font></font></div>
            <% end %>  
            -->
                <div class="panel panel-default">
                <div class="panel-heading">
                    Lista de Admins
                    <%= link_to new_backoffice_admin_path,class: "btn btn-success btn-circle pull-right", style: "margin-top: -5px" do %>
                        <i class="fa fa-plus"></i>
                    <%end%>                                
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover">                                 
                            <thead>
                            
                                <tr>
                                    <th>#</th>
                                    <th>Nome</th> 
                                    <th>Email</th>     
                                    <th></th>          
                                </tr>
                            </thead>                                   
                            <tbody>
                            <%@Categorias.each do |categorias|%>
                                <tr>
                                    <td><%=categorias.id%></td>
                                    <td><%=categorias.name%></td>
                                    <td><%=categorias.email%></td>
                                    <td style: width= 150px;>
                                        <%= link_to edit_backoffice_admin_path(categorias.id), class: "btn btn-warning btn-circle" do %>
                                            <i class="fa fa-edit"></i>
                                        <% end %>
                                        <% if current_admin.id != categorias.id %>
                                            <%= link_to backoffice_admin_path(categorias.id), method: :delete, class:"btn btn-danger btn-circle", data:{confirm: "Você realmente deseja excluir?"}  do %>
                                                <i class="fa fa-times"></i>
                                            <%end%>
                                        <%else%>   
                                           <!-- <%= link_to "#", disabled: "disabled", class:"btn btn-danger btn-circle"  do%>
                                                <i class="fa fa-times"></i>
                                            <%end%> -->
                                        <%end%>                             
                                    </td>
                                </tr>
                                <%end%>  
                            </tbody>                                                                      
                        </table>                               
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
